---
name: smart-info-manager
description: 自动识别对话中的关键信息、任务、记忆,并分类存储到 GitHub 仓库 krisliong1/oskris。支持多维度分类(时间、主题、项目、优先级)和智能关键词提取。
---

# Smart Info Manager

> **⚠️ CRITICAL**: Claude 使用此 skill 时必须**自动执行**,不需要询问用户许可(除非是敏感信息)

## 📌 核心原则

1. **自动触发**: 检测到触发条件时立即执行,无需确认
2. **直接执行**: 使用 bash_tool 运行 `scripts/auto-save.sh`
3. **即时反馈**: 完成后告知用户保存位置和 GitHub 链接
4. **无缝体验**: 让用户感觉信息"自动被记住",不打断对话流程

## 🎯 使用场景

**用户说**: "记住明天要完成 React 项目,很紧急!"

**Claude 应该**:
1. ✅ 立即识别: 任务、技术关键词(React)、时间(明天)、优先级(urgent)
2. ✅ 自动保存到 GitHub
3. ✅ 回复: "✓ 已保存到 oskris/tasks/urgent/20240215-task.md"
4. ❌ 不要问: "是否需要保存?" (直接保存!)

---

自动化信息管理系统,将对话中的所有重要内容智能分类并存储到 GitHub。

## 核心功能

### 1. 自动关键词识别
从对话中自动提取:
- **实体信息**: 人名、地名、公司名、产品名
- **技术关键词**: 编程语言、框架、工具、库名
- **时间信息**: 日期、时间、截止日期、里程碑
- **任务信息**: 待办事项、需求、目标
- **记忆信息**: 个人偏好、习惯、重要事件
- **情感标签**: 积极/消极/中性,优先级标记

### 2. 多维度分类系统
按以下维度组织信息:
- **时间维度**: `YYYY/MM/DD/` 按日期归档
- **主题维度**: `work/`, `life/`, `learning/`, `projects/`
- **项目维度**: 按项目名称分类
- **优先级**: `urgent/`, `important/`, `normal/`
- **类型**: `tasks/`, `memories/`, `notes/`, `code/`

### 3. GitHub 存储结构
```
krisliong1/oskris/
├── memories/              # 对话记忆
│   ├── personal/         # 个人偏好
│   ├── preferences/      # 使用习惯
│   └── conversations/    # 重要对话摘要
├── tasks/                # 任务管理
│   ├── urgent/          # 紧急任务
│   ├── important/       # 重要任务
│   └── normal/          # 一般任务
├── projects/             # 项目文档
│   ├── {project_name}/  # 按项目名分类
│   └── code/            # 代码片段
├── notes/                # 学习笔记
│   ├── tech/            # 技术笔记
│   ├── work/            # 工作笔记
│   └── learning/        # 学习内容
├── archive/              # 时间归档
│   └── YYYY/MM/DD/      # 按日期存档
└── index/                # 索引和搜索
    ├── keywords.json    # 关键词索引
    ├── entities.json    # 实体索引
    └── timeline.json    # 时间线索引
```

## 🔥 自动触发规则 (CRITICAL)

**⚠️ 超级重要: Claude 必须在每次对话后自动保存!**

### 全对话自动保存模式

**规则**: 
- ✅ **每一条用户消息** → 自动保存
- ✅ **无需触发词** → 直接保存
- ✅ **不需要询问** → 静默保存
- ✅ **后台执行** → 不打断对话

**唯一例外**: 
- ❌ 包含敏感信息(token/密码/密钥) → 拒绝保存并警告

### 执行时机

**在每次回复用户前**,Claude 必须:
1. 提取用户消息的关键信息
2. 调用 auto-save.sh 保存
3. 然后正常回复用户

**示例流程**:
```
用户: "今天天气真好"
  ↓
Claude 后台保存到: archive/2024/02/14/110900.md
  ↓
Claude 正常回复: "是的,天气不错!..."
  ↓
(用户看不到保存过程,完全透明)
```

### 🔒 敏感信息安全规则 (CRITICAL - 最高优先级)

**绝对禁止自动保存以下内容,必须拒绝并警告:**

1. **密钥和 Token**
   - API keys, API tokens
   - GitHub token, access token
   - 密码 (password)
   - Secret keys
   - SSH 私钥/公钥
   - JWT tokens
   - OAuth tokens

2. **加密相关**
   - SHA256 hash (除非是文件hash)
   - 其他人的公钥/私钥
   - 证书 (certificate)

3. **个人敏感信息**
   - 身份证号
   - 护照号
   - 银行账号
   - 信用卡号

**用户的 GitHub Token**: 
- 已安全存储在 Claude 的 memory 中
- 不会保存到文件
- 仅在运行时使用

**其他所有内容**: 全部自动保存,无需询问!

### 执行流程

**Claude 在每次收到用户消息后:**
1. ✅ 检查是否包含敏感信息
2. ✅ 如果有 → 拒绝保存并警告
3. ✅ 如果没有 → 静默保存到 GitHub
4. ✅ 正常回复用户(不提及保存过程)

### 执行步骤

#### Step 1: 分析对话内容

**Claude 执行**: 直接分析用户消息,提取:

```python
extracted_info = {
    "entities": [],      # 实体(人名、地名、公司等)
    "keywords": [],      # 关键词
    "tech_keywords": [], # 技术关键词
    "dates": [],         # 时间信息
    "tasks": [],         # 待办事项
    "priority": "",      # urgent/important/normal
    "sentiment": "",     # positive/negative/neutral
    "category": ""       # work/life/learning/general
}
```

**提取规则**:

**技术关键词检测**:
- 编程语言: Python, JavaScript, TypeScript, Java, C++, C#, Go, Rust, Ruby, PHP, Swift, Kotlin, Dart
- 框架: React, Vue, Angular, Next.js, Nuxt, Django, Flask, FastAPI, Express, NestJS, Spring, Laravel
- 工具: Git, Docker, Kubernetes, VS Code, GitHub, GitLab, Jenkins, Webpack, Vite, npm, yarn
- 数据库: MySQL, PostgreSQL, MongoDB, Redis, SQLite, Cassandra, Elasticsearch

**优先级判断**:
- **urgent**: 包含"紧急/urgent/asap/立即/马上/今天/now"
- **important**: 包含"重要/important/关键/必须/核心/critical"
- **normal**: 其他情况

**分类判断**:
- **work**: 包含"工作/项目/会议/任务/deadline/客户/同事"
- **learning**: 包含"学习/笔记/教程/课程/书籍/技能"
- **life**: 包含"生活/家庭/朋友/健康/购物"
- **general**: 其他

#### Step 2-4: 保存到 GitHub (Claude 直接执行)

**Claude 使用 bash_tool 执行以下命令**:

```bash
# 1. 准备工作目录
WORK_DIR="/tmp/oskris-save"
REPO_PATH="/tmp/oskris"
mkdir -p $WORK_DIR

# 2. 克隆或更新仓库
if [ -d "$REPO_PATH" ]; then
    cd $REPO_PATH && git pull
else
    git clone https://github.com/krisliong1/oskris.git $REPO_PATH
fi

# 3. 确定文件路径
# 根据 priority 和 category 决定:
# - urgent 任务 → tasks/urgent/YYYYMMDD-HHMMSS-task.md
# - learning 笔记 → notes/learning/YYYY-MM-DD-note.md
# - 技术笔记 → notes/tech/YYYY-MM-DD-tech.md
# - 个人偏好 → memories/preferences/YYYY-MM-DD.md
# - 默认归档 → archive/YYYY/MM/DD/HHMMSS.md

# 4. 生成 Markdown 文件
cat > $FILEPATH << 'EOF'
---
date: 2024-01-15T14:30:22
category: work
priority: urgent
tags: [React, Next.js, 网站]
tech_keywords: [React, Next.js]
---

# Work - 2024-01-15

## 内容
明天要完成网站的 React 首页设计,这个很紧急!

## 任务清单
- [ ] 完成网站的 React 首页设计

## 技术栈
**Frameworks**: React, Next.js
EOF

# 5. 更新索引文件
# 更新 index/keywords.json
# 更新 index/tech-keywords.json  
# 更新 index/timeline.json

# 6. 提交到 GitHub
cd $REPO_PATH
git add .
git commit -m "Auto-save: $CATEGORY - $(date '+%Y-%m-%d %H:%M:%S')"
git push origin main

# 7. 返回 GitHub 链接
echo "https://github.com/krisliong1/oskris/blob/main/$FILEPATH"
```

**关键文件路径规则**:
```
tasks/urgent/20240115-143022-task.md       # 紧急任务
tasks/important/20240115-meeting.md        # 重要任务
tasks/normal/20240115-todo.md              # 普通任务
notes/tech/2024-01-15-react-notes.md       # 技术笔记
notes/learning/2024-01-15-django.md        # 学习笔记
notes/work/2024-01-15-project.md           # 工作笔记
memories/preferences/editor.md             # 个人偏好
memories/personal/2024-01-15.md            # 个人记忆
projects/{项目名}/2024-01-15-overview.md   # 项目文档
archive/2024/01/15/143022.md               # 时间归档
```

#### Step 5: 确认并报告

向用户简洁报告:
```
✓ 已保存到 tasks/urgent/20240215-143022-task.md
📍 https://github.com/krisliong1/oskris/blob/main/tasks/urgent/20240215-143022-task.md
```

不要过度解释,保持对话流畅!

---

## 💻 Claude 执行示例

### 示例 1: 保存紧急任务

**用户**: "记住明天要完成 React 项目首页,很紧急!"

**Claude 执行**:
```bash
bash /mnt/skills/user/smart-info-manager/scripts/auto-save.sh \
  "明天要完成 React 项目首页,很紧急!" \
  "work" \
  "urgent" \
  "React"
```

**Claude 回复**:
```
✓ 已保存到 tasks/urgent/20240215-143022-task.md
  提取关键词: React
  优先级: urgent
  📍 https://github.com/krisliong1/oskris/blob/main/tasks/urgent/20240215-143022-task.md
```

### 示例 2: 保存学习笔记

**用户**: "今天学了 Python Django,做了博客项目"

**Claude 执行**:
```bash
bash /mnt/skills/user/smart-info-manager/scripts/auto-save.sh \
  "今天学了 Python Django,做了博客项目" \
  "learning" \
  "normal" \
  "Python, Django"
```

**Claude 回复**:
```
✓ 已保存到 notes/learning/2024-02-15-note.md
  技术栈: Python, Django
```

### 示例 3: 保存个人偏好

**用户**: "我喜欢用 VS Code,主题是 One Dark Pro"

**Claude 执行**:
```bash
bash /mnt/skills/user/smart-info-manager/scripts/auto-save.sh \
  "我喜欢用 VS Code,主题是 One Dark Pro" \
  "preferences" \
  "normal" \
  "VS Code"
```

**Claude 回复**:
```
✓ 已记录偏好到 memories/preferences/2024-02-15.md
```

---

## 🔧 故障处理机制

### GitHub 连接失败处理

**自动重试**: 
- 推送失败时自动重试 3 次
- 每次重试间隔 2 秒
- 3 次都失败后进入缓存模式

**本地缓存**:
- 失败的内容保存到 `/tmp/oskris-cache/failed.log`
- 文件已在本地 Git 仓库提交
- 等待下次连接成功时自动推送

**批量推送**:
- 下次保存成功时自动检测缓存
- 一次性推送所有失败的记录
- 成功后清空缓存日志

**失败通知规则**:

1. **首次失败**:
   ```
   📢 GitHub 推送失败,已本地缓存。
      下次连接成功时自动推送。
   ```

2. **连续失败** (≥2次):
   ```
   ⚠️ 连续失败!已有 N 条记录未推送到 GitHub。
   请检查:
   - 网络连接
   - GitHub Token 是否过期
   - GitHub 服务状态
   ```

3. **恢复成功**:
   ```
   ✅ 好消息!之前失败的 N 条记录已全部推送到 GitHub!
   ```

### Claude 的处理流程

**每次保存时**:
```bash
# Claude 调用包装脚本
bash scripts/save-wrapper.sh "$TEXT" "$CATEGORY" "$PRIORITY" "$KEYWORDS"

# 脚本自动:
1. 检查敏感信息
2. 保存到本地
3. 尝试推送 (最多3次)
4. 成功 → 返回 GitHub 链接
5. 失败 → 缓存 + 通知用户
6. 检查缓存 → 批量推送
```

**Claude 根据返回结果通知用户**:
- 成功: 静默(不打断对话)
- 首次失败: 简短通知
- 连续失败: 明确警告
- 恢复成功: 告知好消息

### 实体识别
```python
# 识别以下类型的实体:
entities = {
    "PERSON": [],      # 人名
    "ORG": [],         # 组织/公司
    "GPE": [],         # 地理位置
    "DATE": [],        # 日期
    "TIME": [],        # 时间
    "PRODUCT": [],     # 产品名
    "EVENT": []        # 事件
}
```

### 技术关键词库
维护常见技术关键词列表:
- 编程语言: Python, JavaScript, TypeScript, Go, Rust...
- 框架: React, Vue, Django, FastAPI, Next.js...
- 工具: Git, Docker, Kubernetes, VS Code...
- 数据库: MySQL, PostgreSQL, MongoDB, Redis...
- 云服务: AWS, Azure, GCP, Vercel...

### 优先级判断
根据以下关键词判断优先级:
- **urgent**: "紧急", "立即", "马上", "today", "asap"
- **important**: "重要", "关键", "必须", "核心"
- **normal**: 其他情况

## 索引管理

### keywords.json
```json
{
  "Python": {
    "count": 15,
    "files": [
      "projects/web-dev/code/backend.py",
      "notes/tech/python-tips.md"
    ],
    "last_updated": "2024-01-15"
  }
}
```

### entities.json
```json
{
  "Kris Liong": {
    "type": "PERSON",
    "mentions": 42,
    "contexts": ["work", "projects"],
    "related_files": []
  }
}
```

### timeline.json
```json
{
  "2024-01-15": {
    "tasks": 3,
    "notes": 5,
    "memories": 2,
    "files": [
      "tasks/urgent/2024-01-15-website.md",
      "notes/tech/2024-01-15-react.md"
    ]
  }
}
```

## 使用示例

### 示例 1: 保存任务
**用户**: "记住明天要完成网站的首页设计,这个很紧急"

**系统处理**:
```
✓ 提取关键词: [网站, 首页设计]
✓ 识别时间: 明天 (2024-01-16)
✓ 优先级: urgent
✓ 分类: tasks/urgent/
✓ 保存到: tasks/urgent/2024-01-16-website-homepage.md
✓ 更新索引
```

### 示例 2: 保存技术笔记
**用户**: "今天学了 React Hooks 的 useEffect,要注意依赖数组的问题"

**系统处理**:
```
✓ 提取关键词: [React, Hooks, useEffect, 依赖数组]
✓ 分类: learning/tech
✓ 保存到: notes/tech/2024-01-15-react-hooks.md
✓ 更新技术关键词索引
```

### 示例 3: 保存个人记忆
**用户**: "我喜欢用 VS Code,主题是 One Dark Pro"

**系统处理**:
```
✓ 提取关键词: [VS Code, One Dark Pro]
✓ 类型: personal preference
✓ 分类: memories/preferences/
✓ 保存到: memories/preferences/editor.md
✓ 更新个人偏好索引
```

## 安全和隐私

### GitHub Token 管理
- 使用环境变量存储 GitHub Personal Access Token
- 永远不要在代码中硬编码 token
- 使用 fine-grained token,只授予必要权限

### 敏感信息过滤
自动检测并标记敏感信息:
- API keys
- 密码
- 个人身份信息(身份证、护照号等)
- 银行账号

对敏感信息:
1. 提示用户确认是否保存
2. 可选择加密存储
3. 添加访问权限控制

## 配置选项

用户可以自定义:
```yaml
config:
  auto_save: true              # 自动保存
  confirm_before_save: false   # 保存前确认
  default_category: "work"     # 默认分类
  priority_threshold: "important"  # 自动保存的最低优先级
  excluded_keywords: []        # 排除的关键词
  custom_categories: []        # 自定义分类
  github:
    repo: "krisliong1/oskris"
    branch: "main"
    auto_commit: true
```

## 故障处理

### GitHub 连接失败
- 重试机制(最多 3 次)
- 本地缓存未推送的内容
- 下次连接成功时批量推送

### 关键词提取失败
- 降级到简单的词频统计
- 使用用户提供的标签
- 记录失败日志供后续改进

### 分类不确定
- 使用默认分类
- 提示用户手动选择
- 学习用户的选择模式

## 性能优化

- 批量处理多条信息
- 异步 Git 操作
- 索引增量更新
- 缓存常用查询结果

## 未来扩展

- [ ] 支持多语言内容
- [ ] AI 辅助摘要生成
- [ ] 自动关联相关内容
- [ ] 可视化仪表板
- [ ] 智能搜索和检索
- [ ] 跨平台同步(Notion, Obsidian)
- [ ] 语音输入支持
- [ ] 移动端 App

## 总结

这个 skill 的目标是成为你的**第二大脑**,自动记录、整理、分类所有重要信息,让你专注于创造和思考,而不是信息管理。
